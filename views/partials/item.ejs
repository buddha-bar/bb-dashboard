<div class="item clearfix" ng-controller="ItemController as store" >
  <div ng-repeat="item in store.item">
    <img ng-src="{{item.img}}" />
    <h2>{{item.name}}</h2>
    <p> Stock level: {{item.stock}}</p>
    <div class="clear"></div>
  </div>
</div>
